scalar Time

type User {
    id: ID!
    createdAt: Time
    age: Int!
    names: String!
    lastnames: String!
    activate: Boolean!
    birthday: Time
}

input UserInput {
    age: Int!
    birthday: Time
    names: String!
    lastnames: String!
    activate: Boolean!
}

type Mutation {
    createUser(user: UserInput!): User!
}

type Query {
    users: [User!]
}